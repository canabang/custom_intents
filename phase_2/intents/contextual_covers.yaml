# ==========================================================
# PHRASES : VOLETS CONTEXTUELS (PHASE 2)
# ==========================================================
# Pilotage des volets sans spécifier la pièce.
# ==========================================================

language: "fr"
intents:
  TurnOnContextualCover:
    data:
      - sentences:
          - "ouvre [le|les] volet[s]"
          - "monte [le|les] volet[s]"
          - "ouvre tout"
          - "que le jour se lève"
  
  TurnOffContextualCover:
    data:
      - sentences:
          - "ferme [le|les] volet[s]"
          - "descends [le|les] volet[s]"
          - "ferme tout"
          - "mode buncker"
